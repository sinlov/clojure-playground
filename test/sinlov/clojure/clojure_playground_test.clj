(ns sinlov.clojure.clojure-playground-test
  (:require [clojure.test :refer :all]
            [sinlov.clojure.clojure-playground :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
